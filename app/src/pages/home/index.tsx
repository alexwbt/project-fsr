import { Container } from "react-bootstrap";
import MainTable from "./MainTable";

const Home: React.FC = () => {
    return <Container fluid>
        <MainTable />
    </Container>;
};

export default Home;
